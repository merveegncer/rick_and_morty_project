import 'package:flutter/material.dart';

class ChipDesign extends StatelessWidget {
  const ChipDesign({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
